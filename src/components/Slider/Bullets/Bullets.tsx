import React, { FC } from "react";
import { BulletTogglersSettings } from "../Types/types";

const Bullets: FC<BulletTogglersSettings> = ({ type }) => {
  return <div></div>;
};

export default Bullets;
