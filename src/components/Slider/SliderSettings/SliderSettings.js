export default class SliderSettings {
    constructor(slides, togglers, showSlides, arrows) {
        this.slides = slides;
        this.togglers = togglers;
        this.showSlides = showSlides;
        this.arrows = arrows;
    }
}